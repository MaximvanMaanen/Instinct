@{
    ViewBag.Title = "Home Page";
}

<div class="container">
    <div class="row">
        <div class="col-md-6">
            <h2 style="color: white;">Place a Donation Request</h2>
            <p>
                Lorem ipsum.
            </p>
            <p class="btn btn-text btn-primary">@Html.ActionLink("Donate", "Donation", "Donation")</p>
        </div>
        <div class="col-md-6">
            <h2 style="color: white;">Check the Donation Progress</h2>
            <p>
                Lorem ipsum.
            </p>
            @Html.ActionLink("Home", "Index", "Donation", new { @class = "btn btn-text btn-primary" })
        </div>
    </div>
</div>